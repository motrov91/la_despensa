import 'package:flutter/material.dart';

class AseoPersonalScreen extends StatelessWidget {

  static const String routeName = 'aseopersonal';
   
  const AseoPersonalScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('AseoPersonalScreen'),
      ),
    );
  }
}

